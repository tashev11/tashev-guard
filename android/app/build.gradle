plugins {
    id "com.android.application"
}

android {
    namespace "dev.tashev.guard"
    compileSdk 36

    defaultConfig {
        applicationId "dev.tashev.guard"
        minSdk 26
        targetSdk 36
        versionCode 1
        versionName "0.1.0"
    }
}
